apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: api-governance-gateway
  namespace: api-governance
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - api-governance.local
        - "*"
